import { FilterProvincePipe } from './filter-province.pipe';

describe('FilterProvincePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProvincePipe();
    expect(pipe).toBeTruthy();
  });
});
